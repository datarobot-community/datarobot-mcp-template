---
name: Test mcp internal library

on:
  push:
    branches: ["main"]
    paths:
      - '.internal/dr-mcp-server-lib/**.py'

  pull_request:
    branches: ["main"]
    paths:
      - '.internal/dr-mcp-server-lib/**.py'

jobs:
  tests:
    name: "FastMCP: .internal/dr-mcp-server-lib ${{ matrix.python-version }}"
    runs-on: "ubuntu-latest"
    defaults:
      run:
        working-directory: .internal/dr-mcp-server-lib

    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]

    steps:
      - uses: "actions/checkout@v5"

      # Ref: https://docs.astral.sh/uv/guides/integration/github/#installation
      - name: Setup uv
        uses: astral-sh/setup-uv@v7
        with:
          working-directory: .internal/dr-mcp-server-lib
          enable-cache: true
          cache-dependency-glob: "uv.lock"
          python-version: ${{ matrix.python-version }}

      - name: Install Taskfile
        uses: arduino/setup-task@v2
        with:
          version: 3.x
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install Dependencies
        working-directory: .internal/dr-mcp-server-lib
        run: task install

      - name: Run Static Checks
        working-directory: .internal/dr-mcp-server-lib
        run: task lint-check

      - name: Run Unit Tests
        working-directory: .internal/dr-mcp-server-lib
        run: task unit

      - name: Get Cover
        uses: orgoro/coverage@v3.2
        with:
          coverageFile: .internal/dr-mcp-server-lib/coverage.xml
          token: ${{ secrets.GITHUB_TOKEN }}
